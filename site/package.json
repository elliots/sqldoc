{
  "name": "sqldoc-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "generate": "bash ../tests/pet-store-postgres/generate-all-templates.sh && bun data/generate-cli-reference.ts && SQLDOC_RESOLVE_FROM_LOCAL_PACKAGE=true bun ../packages/cli/src/index.ts compile examples/homepage.sql -c examples/sqldoc.config.ts -o homepage-compiled.sql && bun data/generate-homepage-example.ts",
    "build": "bun run generate && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "1.6.4",
    "vitepress-carbon": "1.6.0"
  }
}
