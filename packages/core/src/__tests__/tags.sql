-- @for("one")
create table one ( -- @for("one again")

    -- @for("two")
    two text, -- @for("two again")
    three text, -- @for("three")

    -- @for("four")


four numeric

    -- @for("one yet again")

)
